const {getData, saveData, findAndUpdate, insertMany, removeMany, countData, populateData, saveSessionData, deleteData,countDocuments} = require('./queries')

module.exports = {
     getData,
     saveData, 
     findAndUpdate,
     insertMany,
     removeMany,
     countData,
     populateData,
     saveSessionData,
     deleteData,
     countDocuments
    }